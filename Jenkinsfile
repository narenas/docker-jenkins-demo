pipeline{
    agent {
        dockerfile true
    }
    stages{
        stage("build") {
            steps{
                echo "========Running Container========"
           }
        }
        stage("Run tests") {
            steps{
                sh "echo =============Runing tests==========" 
            }
        }
    }
}
